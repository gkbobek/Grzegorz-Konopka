#include "stdafx.h"
#include <iostream>
using namespace std;

int main()
{
	int n;
	cout << "Give me a number: ";
	cin >> n;
	int sum = 1;
	for (int i = 1; i <= n; i++)
	{
		if (i % 2 == 0)
		{
			sum *= i;
		}
	}
	cout << sum;
	cin.get();
	cin.get();
    return 0;
}

